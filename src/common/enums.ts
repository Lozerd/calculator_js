export enum ActionType {
    EMPTY = "",
    ADD = "+",
    SUBTRACT = "-",
    DIVIDE = "/",
    MULTIPLY = "×",
    DECIMAL = ".",
    NUMBER = "number"
}
export enum MouseState {
    MOVE = "mousemove",
    LEAVE = "mouseleave"
}